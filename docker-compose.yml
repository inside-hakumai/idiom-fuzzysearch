version: "3"

services:

  web:
    build: web/
    ports:
      - "3000:3000"
    volumes:
      - ./web/bin:/app/bin
      - ./web/src:/app/src
      - ./web/tsconfig.json:/app/tsconfig.json

  db:
    build: db/
    ports:
      - "5432:5432"
